{
  "name": "SMTP Provider",
  "type": "smtp",
  "data": {
    "host": "={{ $env.SMTP_HOST }}",
    "port": "={{ $env.SMTP_PORT }}",
    "secure": true,
    "user": "={{ $env.SMTP_USER }}",
    "password": "={{ $env.SMTP_PASS }}"
  },
  "nodesAccess": [
    { "node": "Send Email" }
  ],
  "notes": "SMTP email provider for sending notifications. Configure SMTP_HOST, SMTP_PORT, SMTP_USER, SMTP_PASS in n8n environment variables.",
  "createdAt": "2026-06-29",
  "updatedAt": "2026-06-29"
}
